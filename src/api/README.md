# Ecotrack GraphQL API


## Development server

Start container with `docker run -it -p 4000:4000 -v ${pwd}/src:/app/src kleky/ecotrack-api`

Go to `localhost:4000`

## Build

Run `docker build -t kleky/ecotrack-api .`
